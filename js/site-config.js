/* 실제 전환 정보 — 확정 시 여기 한 곳만 바꾸면 전 페이지 CTA에 반영됩니다 (widgets.js가 주입).
   ⚠ 아래 값은 전부 "가라(더미)" — 개원 확정 시 실제 값으로 교체할 것. */
window.BARO_CONFIG = {
  tel: "031-381-7528",                                       // 가라 — 실제 대표번호로 교체
  naverBooking: "https://booking.naver.com/booking/13/bizes/1187423",  // 가라 — 실제 네이버 예약 URL로 교체
  kakaoChannel: "https://pf.kakao.com/_yonseibaro",          // 가라 — 실제 카카오 채널 URL로 교체
  address: "경기 안양시 동안구 시민대로 187, 4층 (호계동)",   // 가라 — 실제 주소로 교체
  mapEmbedUrl: "https://maps.google.com/maps?q=%EB%B2%94%EA%B3%84%EC%97%AD&z=16&output=embed&hl=ko", // 가라(범계역) — 개원 시 네이버/카카오 지도 임베드로 교체
  ga4: ""                                                    // GA4 측정 ID(G-XXXX) 확정 시 입력 → 트래킹 자동 활성
};
